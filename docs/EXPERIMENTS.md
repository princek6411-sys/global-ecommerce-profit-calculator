# ProfitPilot Experiments

Experiment lifecycle:

HYPOTHESIS → BASELINE → CHANGE → OBSERVE → RESULT → LEARNING

What-If output is a scenario. It is not an observed outcome.

The current implementation stores experiment state locally in the browser. Durable multi-device history requires persistence and authentication.
