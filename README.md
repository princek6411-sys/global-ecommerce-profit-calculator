# ProfitPilot Elite Landing Page

A Next.js 14 App Router landing page for ProfitPilot with a responsive, interactive homepage prototype.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The page is intentionally self-contained in `app/page.tsx` and uses Tailwind CSS.
